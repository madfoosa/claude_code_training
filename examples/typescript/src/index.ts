/**
 * TypeScript slice exercising the Claude Code development kit.
 *
 * This package exists to give the kit something real to lint, typecheck, test,
 * break, and fix. It is mirrored feature-for-feature by `examples/python` --
 * see the "Both stacks mirror each other" rule in `CLAUDE.md`.
 */

export { TokenBucket } from './bucket.js';
export type { Clock } from './bucket.js';
